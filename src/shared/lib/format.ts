export function formatFaNumber(value: number) {
  return new Intl.NumberFormat("fa-IR").format(value);
}

export function formatFaCurrency(value: number) {
  return new Intl.NumberFormat("fa-IR").format(value);
}

